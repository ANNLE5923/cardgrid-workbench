# 私人分发与 GitHub 上传

## 私人分发

分发源码 ZIP，不分发浏览器配置目录或完整备份。接收人解压后安装 Node.js 22 或更高版本，双击“启动卡格工作台.bat”；首次启动会执行 npm ci 安装依赖。

每个人在自己的浏览器来源下建立独立 IndexedDB。通过相同网址访问的浏览器配置会共用该工作区；不同端口和设备不会自动同步。个人完整备份应由本人单独保存和传递。

源码压缩包不含 node_modules、dist、测试用个人原型、截图或本机数据库。发布 ZIP 前可用压缩包列表复核内容。

## 上传到 GitHub

仓库根目录就是当前 CardGrid 项目文件夹。项目已初始化本地 main 分支和忽略规则；远程仓库尚未创建，也未配置 remote 或推送。

创建 GitHub 仓库时可先设为 Private。上传前复核 Git 变更列表，确认没有个人备份、真实日程导出、截图、浏览器数据或密钥。公开前还需选择并添加许可证；当前未附许可证，因此应按保留所有权处理。

GitHub Actions 会在推送和 Pull Request 时执行 npm ci、npm test、npm run build。构建已使用相对资源路径，适用于 GitHub Pages 项目子路径；这不会自动启用 Pages。启用 Pages 前需确认仓库内容适合公开，且只发布静态应用文件。

## 已知分发要求

- 需要 Node.js 22+ 和可访问 npm registry 的网络完成首次依赖安装。
- 启动脚本使用 localhost:4173；同一端口已有其他程序运行时会启动失败。
- PWA 离线缓存服务端构建后生效；开发服务器不能代表部署后的离线行为。
- 备份文件含私人数据。不要把它添加到仓库或源码 ZIP。
